import random

#really advanced ai powered random number generator that can predict the future and read your mind, 
#but only for numbers between 1 and 10.
def fun():
   return random.randint(1, 100)


print ("")
user_input = int(input("Guess what number Im thinking of between 1 and 10:"))
print ("")

random_number = fun()

if user_input == random_number:
    print ("Wow you guessed it! The number was indeed " + str(random_number))
else:
    print ("Sorry, you guessed " + str(user_input) + " but the number was actually " + str(random_number))


